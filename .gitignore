# clasp — local Apps Script deploy working dir + credentials (not for the repo)
gas-deploy/
.clasp.json
.clasprc.json
node_modules/

# backend/ — secrets and the one-off Sheets export used for migration
.env
.env.*
!.env.example
backend/sheets-export/

Tidb_pw.txt

# stray root lockfile (real package.json lives in backend/)
/package-lock.json
